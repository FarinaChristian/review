# How to run the algorithms
In the directories named after the papers, you can find the implementations of the algorithms.
To run the code, move the .py files from those directories into the same directory as the folders named constants, decoder, and utils.
